# Scripts & Ingestion Design

## Overview
The `scripts/` directory contains utility tools for maintaining the bot. 

## Key Components


### Maintenance Tools
*   **`check_env.py`**: Validates that `.env` exists and has all required keys (`DISCORD_TOKEN`, `GEMINI_API_KEY`, `LLM_PROVIDER`).

